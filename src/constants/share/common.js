import styled from 'styled-components'

export const ImgShadow = styled.div`
  width: fit-content;
  height: fit-content;
  overflow: hidden;
  border-radius: 0.5rem;
  box-shadow: 0 2px 40px 0 rgba(0, 21, 73, 0.2);
  -webkit-box-shadow: 0 2px 40px 0 rgba(0, 21, 73, 0.2);

  > svg {
    font-family: inter;
  }
`;
